'use client';

// Kept as a compatibility entry point for older imports. The competition now
// uses the fixed rules shown by TournamentRulesCard and has no editable phase
// configuration.
export default function PhaseConfig() {
  return null;
}
